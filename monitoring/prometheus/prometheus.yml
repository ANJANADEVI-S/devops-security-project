global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:

  - job_name: 'vault-custom-exporter'
    static_configs:
      - targets: ['localhost:9100']

  # Uncomment on integration day when M3's Vault is reachable
  # - job_name: 'vault'
  #   metrics_path: '/v1/sys/metrics'
  #   params:
  #     format: ['prometheus']
  #   bearer_token: 'FILL_IN_ON_INTEGRATION_DAY'
  #   static_configs:
  #     - targets: ['FILL_IN_VAULT_HOST:8200']